apiVersion: 1
datasources:
  - name: SQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: false
    jsonData:
      path: /var/lib/grafana/data/sqlite/attributed_page_views.db?cache=shared&mode=ro